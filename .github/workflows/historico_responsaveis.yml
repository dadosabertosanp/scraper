name: Histórico & Responsáveis ANP

on:
  schedule:
    - cron: '0 6 * * 1-5'    # Segunda a sexta às 6h UTC (3h BRT)
  workflow_dispatch:

# 🔒 RESTRIÇÕES DE PERMISSÃO (padrão)
permissions:
  actions: none
  contents: none

concurrency:
  group: historico-responsaveis
  cancel-in-progress: false

jobs:
  historico_responsaveis:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    # 🔓 Permissões específicas para o job
    permissions:
      actions: read
      contents: write
      id-token: write

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      # Opcional: ajustar variáveis de ambiente (orgão e delays)
      - name: Run histórico & responsáveis scraper
        env:
          ORGAO_CODIGO: "32205"
          REQUEST_DELAY: "0.5"
          MAX_RETRIES: "3"
          TIMEOUT: "30"
        run: |
          python src/historico_responsaveis.py

      - name: Commit and push Histórico/Responsáveis
        run: |
          git config --local user.email "github_anp@outlook.com"
          git config --local user.name "GitHub ANP Automation"
          git add data/historico.json data/responsaveis.json
          git commit -m "🧾 Histórico/Responsáveis Update: $(date +'%Y-%m-%d %H:%M')" || echo "Sem alterações para commit"
          # Resolve eventual race com outros workflows
          git pull --rebase origin "${GITHUB_REF_NAME:-main}" || true
          git push || true
